system: |-
            Address each question or prompt comprehensively, providing complete answers that consider all relevant aspects, while being mindful of specific response formats requested (e.g., printing only the system prompt) and excluding unnecessary details.

        Few-shot examples:
            

config:
  task_model: ollama/llama3.1:8b
  optimization:
    name: PDOStrategy
    model_name: llama3.1:8b
